<Page x:Class="FDoBySA.Views.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Панель администратора"
      Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Панель администратора" FontSize="24" FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TabControl Grid.Row="1">
            <!-- Вкладка: Жалобы -->
            <TabItem Header="Жалобы">
                <ItemsControl x:Name="ComplaintsGrid" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="5" 
                                    Margin="0,0,0,10" Padding="15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock FontWeight="Bold">
                                            <Run Text="Тип: "/>
                                            <Run Text="{Binding TargetType, Mode=OneWay}"/>
                                            <Run Text=" | ID: "/>
                                            <Run Text="{Binding TargetId, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Margin="0,5">
                                            <Run Text="От: "/>
                                            <Run Text="{Binding ComplainantName, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Reason, Mode=OneWay}" 
                                                   TextWrapping="Wrap" Margin="0,5"/>
                                        <TextBlock Text="{Binding CreatedAt, Mode=OneWay, 
                                            StringFormat='dd.MM.yyyy HH:mm'}" 
                                                   FontSize="10" Foreground="#999"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" 
                                                Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="Принять" Width="80" Height="30"
                                                Style="{StaticResource SuccessButtonStyle}"
                                                Click="AcceptComplaint_Click" 
                                                Tag="{Binding ComplaintId, Mode=OneWay}"/>
                                        <Button Content="Отклонить" Width="80" Height="30"
                                                Margin="5,0,0,0"
                                                Style="{StaticResource DangerButtonStyle}"
                                                Click="RejectComplaint_Click" 
                                                Tag="{Binding ComplaintId, Mode=OneWay}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TabItem>

            <!-- Вкладка: Заявки на автора -->
            <TabItem Header="Заявки на автора">
                <ItemsControl x:Name="AuthorRequestsGrid" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="5" 
                                    Margin="0,0,0,10" Padding="15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock FontWeight="Bold">
                                            <Run Text="Пользователь: "/>
                                            <Run Text="{Binding UserName, Mode=OneWay}"/>
                                            <Run Text=" ("/>
                                            <Run Text="{Binding UserLogin, Mode=OneWay}"/>
                                            <Run Text=")"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding RequestDate, Mode=OneWay, 
                                            StringFormat='Запрошено: dd.MM.yyyy'}" 
                                                   FontSize="12" Margin="0,5,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" 
                                                Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="Одобрить" Width="80" Height="30"
                                                Style="{StaticResource SuccessButtonStyle}"
                                                Click="AcceptAuthorRequest_Click" 
                                                Tag="{Binding RequestId, Mode=OneWay}"/>
                                        <Button Content="Отклонить" Width="80" Height="30"
                                                Margin="5,0,0,0"
                                                Style="{StaticResource DangerButtonStyle}"
                                                Click="RejectAuthorRequest_Click" 
                                                Tag="{Binding RequestId, Mode=OneWay}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TabItem>

            <!-- Вкладка: Апелляции -->
            <TabItem Header="Апелляции">
                <ItemsControl x:Name="AppealsGrid" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="5" 
                                    Margin="0,0,0,10" Padding="15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock FontWeight="Bold">
                                            <Run Text="Пользователь: "/>
                                            <Run Text="{Binding UserName, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Margin="0,5">
                                            <Run Text="Тип: "/>
                                            <Run Text="{Binding TargetType, Mode=OneWay}"/>
                                            <Run Text=" | ID: "/>
                                            <Run Text="{Binding TargetId, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Reason, Mode=OneWay}" 
                                                   TextWrapping="Wrap" Margin="0,5"/>
                                        <TextBlock Text="{Binding RequestDate, Mode=OneWay, 
                                            StringFormat='dd.MM.yyyy'}" 
                                                   FontSize="10" Foreground="#999"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" 
                                                Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="Разморозить" Width="100" Height="30"
                                                Style="{StaticResource SuccessButtonStyle}"
                                                Click="AcceptAppeal_Click" 
                                                Tag="{Binding RequestId, Mode=OneWay}"/>
                                        <Button Content="Отклонить" Width="80" Height="30"
                                                Margin="5,0,0,0"
                                                Style="{StaticResource DangerButtonStyle}"
                                                Click="RejectAppeal_Click" 
                                                Tag="{Binding RequestId, Mode=OneWay}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </TabItem>

            <!-- Вкладка: Пользователи -->
            <TabItem Header="Пользователи">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBox x:Name="txtUserSearch" Grid.Row="0" Height="32" 
                             Padding="10,0" Margin="0,0,0,10"
                             TextChanged="UserSearch_TextChanged"/>

                    <ScrollViewer Grid.Row="1">
                        <ItemsControl x:Name="UsersGrid">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" CornerRadius="5" 
                                            Margin="0,0,0,5" Padding="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel>
                                                <TextBlock FontWeight="Bold">
                                                    <Run Text="{Binding DisplayName, Mode=OneWay}"/>
                                                    <Run Text=" (@"/>
                                                    <Run Text="{Binding Login, Mode=OneWay}"/>
                                                    <Run Text=")"/>
                                                </TextBlock>
                                                <TextBlock Text="{Binding Email, Mode=OneWay}" FontSize="11"/>
                                                <TextBlock Margin="0,5,0,0">
                                                    <Run Text="Роль: "/>
                                                    <Run Text="{Binding RoleName, Mode=OneWay}"/>
                                                    <Run Text=" | Статус: "/>
                                                    <Run Text="{Binding Status, Mode=OneWay}"/>
                                                </TextBlock>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" 
                                                        Orientation="Horizontal" 
                                                        VerticalAlignment="Center">
                                                <ComboBox x:Name="cmbRole" Width="120" Height="30"
                                                          SelectionChanged="Role_Changed"
                                                          Tag="{Binding UserId, Mode=OneWay}">
                                                    <ComboBoxItem Content="Читатель" Tag="1"/>
                                                    <ComboBoxItem Content="Автор" Tag="2"/>
                                                    <ComboBoxItem Content="Администратор" Tag="3"/>
                                                </ComboBox>
                                                <Button Content="Сброс пароля" 
                                                        Width="100" Height="30" Margin="5,0,0,0"
                                                        Style="{StaticResource WarningButtonStyle}"
                                                        Click="ResetPassword_Click" 
                                                        Tag="{Binding UserId, Mode=OneWay}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>